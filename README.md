# Elephant Color Shop 🐘

Веб-приложение для покупки уникальных изображений слонов, закрашенных определённым цветом из палитры RGB. Каждый цвет может быть куплен только один раз!

## Технологический стек

- **Backend**: Django 5.x + django-ninja (REST API)
- **Async задачи**: Celery + Redis
- **База данных**: PostgreSQL 16
- **Кэш/Брокер**: Redis 7
- **Frontend**: Alpine.js + DaisyUI (Tailwind CSS)
- **Web Server**: Nginx + Gunicorn
- **SSL**: Let's Encrypt (автообновление через certbot)
- **Контейнеризация**: Docker + docker-compose
- **Генерация изображений**: cairosvg (SVG → PNG)
- **CI/CD**: GitHub Actions

## Особенности

- ✅ Уникальные цвета - каждый HEX код может быть куплен только один раз
- ✅ Два тарифа: Basic (случайный цвет) и Advanced (выбор цвета)
- ✅ Асинхронная генерация изображений через Celery
- ✅ Система подарков с публичными UUID ссылками
- ✅ RESTful API с автодокументацией
- ✅ Fat Models подход - бизнес-логика в моделях и сервисах
- ✅ Docker для простого развёртывания

## Быстрый старт

### Локальная разработка

#### Требования

- Docker и Docker Compose

#### Установка и запуск

1. Клонируйте репозиторий:
```bash
cd /Users/hvosdt/Documents/dev/kupi_slona
```

2. Скопируйте .env.example в .env (уже сделано):
```bash
cp .env.example .env
```

3. Запустите проект через Docker Compose:
```bash
docker-compose up --build
```

4. В отдельном терминале создайте суперпользователя:
```bash
docker-compose exec web python manage.py createsuperuser
```

5. Создайте тарифы через admin или Django shell:
```bash
docker-compose exec web python manage.py shell
```

Затем в shell:
```python
from apps.payments.models import Tariff

Tariff.objects.create(name="basic", price=9.99, description="Слон со случайным цветом", is_active=True)
Tariff.objects.create(name="advanced", price=19.99, description="Слон с выбранным цветом", is_active=True)
```

6. Приложение доступно по адресам:
   - **API документация**: http://localhost:8000/api/docs
   - **Admin панель**: http://localhost:8000/admin
   - **API**: http://localhost:8000/api/

### Production деплой

Полная документация по развёртыванию на production сервере с SSL: **[DEPLOYMENT.md](DEPLOYMENT.md)**

**Краткий обзор:**

1. **Настройка домена** - направьте A-запись на IP сервера
2. **Первоначальная настройка**:
   ```bash
   cd ~/kupi_slona
   cp .env.example .env
   nano .env  # Настройте переменные окружения
   ```

3. **Получение SSL сертификата**:
   ```bash
   nano init-letsencrypt.sh  # Укажите домен и email
   ./init-letsencrypt.sh
   ```

4. **Автоматический деплой через GitHub Actions** - настройте secrets и пушьте в `main`

5. **Ручной деплой**:
   ```bash
   ./auto-deploy.sh
   ```

**Production стек:**
- Nginx (SSL termination, static/media files)
- Gunicorn (WSGI server)
- PostgreSQL (база данных)
- Redis (Celery broker)
- Certbot (автообновление SSL)

## Структура проекта

```
elephant_shop/
├── config/                  # Django настройки и конфигурация
│   ├── settings.py          # Основные настройки
│   ├── urls.py              # URL маршруты
│   ├── celery.py            # Настройка Celery
│   └── wsgi.py              # WSGI конфигурация
├── apps/                    # Django приложения
│   ├── accounts/            # Аутентификация
│   ├── elephants/           # Модели и логика слонов
│   ├── gifts/               # Система подарков
│   └── payments/            # Заказы и тарифы
├── templates/               # HTML шаблоны
├── static/                  # Статические файлы
├── media/                   # Загруженные файлы (изображения слонов)
├── nginx/                   # Nginx конфигурация
│   ├── nginx.conf           # Основная конфигурация nginx
│   └── conf.d/              # Server blocks
│       └── default.conf     # Конфигурация сайта (HTTP/HTTPS)
├── docker-compose.yml       # Development конфигурация
├── docker-compose.prod.yml  # Production конфигурация
├── Dockerfile               # Docker образ приложения
├── auto-deploy.sh           # Скрипт ручного деплоя
├── init-letsencrypt.sh      # Скрипт настройки SSL
└── requirements.txt         # Python зависимости
```

## API Endpoints

### Аутентификация

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь

### Тарифы и Заказы

- `GET /api/tariffs` - Список активных тарифов
- `POST /api/orders` - Создать заказ
- `GET /api/orders` - Список заказов пользователя
- `GET /api/orders/{id}` - Детали заказа
- `POST /api/orders/{id}/pay` - Оплатить заказ (запускает генерацию слона)

### Слоны

- `GET /api/elephants/` - Список слонов пользователя
- `GET /api/elephants/{id}` - Детали слона
- `GET /api/elephants/{id}/download` - Скачать изображение

### Подарки

- `POST /api/gifts/` - Создать подарочную ссылку
- `GET /api/gifts/sent` - Список отправленных подарков
- `GET /api/gifts/public/{uuid}` - Публичная информация о подарке
- `POST /api/gifts/public/{uuid}/claim` - Принять подарок

## Пример использования API

### 1. Регистрация
```bash
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john",
    "email": "john@example.com",
    "password": "securepass123"
  }' \
  -c cookies.txt
```

### 2. Создание заказа (Advanced тариф с выбором цвета)
```bash
curl -X POST http://localhost:8000/api/orders \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{
    "tariff_name": "advanced",
    "desired_color": "#FF5733"
  }'
```

### 3. Оплата заказа (запуск генерации)
```bash
curl -X POST http://localhost:8000/api/orders/1/pay \
  -b cookies.txt
```

### 4. Получение списка слонов
```bash
curl -X GET http://localhost:8000/api/elephants/ \
  -b cookies.txt
```

### 5. Создание подарка
```bash
curl -X POST http://localhost:8000/api/gifts/ \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{
    "elephant_id": 1,
    "recipient_name": "Jane",
    "message": "С днём рождения!"
  }'
```

## Архитектура

### Fat Models
Бизнес-логика находится в моделях:
- Валидация данных в методе `clean()`
- Бизнес-методы (`mark_as_paid()`, `can_be_gifted()`, etc.)
- Автоматическая обработка данных в `save()`

### Сервисный слой
Сложная бизнес-логика вынесена в сервисы:
- `apps/elephants/services.py` - работа со слонами
- `apps/payments/services.py` - заказы и оплата
- `apps/gifts/services.py` - подарки

### Celery задачи
Асинхронная генерация изображений:
- `apps/elephants/tasks.py::generate_elephant_image` - генерация слона после оплаты

## Development

### Без Docker (локальная разработка)

1. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите PostgreSQL и Redis локально или используйте только сервисы из Docker:
```bash
docker-compose up db redis
```

4. Примените миграции:
```bash
python manage.py migrate
```

5. Запустите dev сервер:
```bash
python manage.py runserver
```

6. В отдельном терминале запустите Celery worker:
```bash
celery -A config worker --loglevel=info
```

## Тестирование

Запустите тесты:
```bash
docker-compose exec web python manage.py test
```

## Admin панель

Доступна по адресу: http://localhost:8000/admin

Возможности:
- Управление пользователями
- Просмотр и редактирование тарифов
- Мониторинг заказов
- Просмотр слонов с цветовыми превью
- Управление подарочными ссылками

## Безопасность

- CSRF защита для всех форм
- Проверка владельца при операциях с ресурсами
- Session-based аутентификация
- Уникальность цветов на уровне БД (constraint)
- Транзакции для критичных операций

## Производительность

- Redis кэширование
- Асинхронная генерация изображений (не блокирует API)
- Database indexes на часто запрашиваемых полях
- select_related для оптимизации запросов

## TODO / Будущие улучшения

- [x] Frontend с Alpine.js и DaisyUI
- [x] CI/CD pipeline (GitHub Actions)
- [x] Production деплой с SSL
- [ ] Реальная платёжная интеграция (Stripe/PayPal)
- [ ] Email уведомления
- [ ] Поиск доступных цветов
- [ ] История изменений владельца слона
- [ ] Rate limiting для API
- [ ] Расширенное тестирование

## Лицензия

MIT

## Автор

Создано с помощью Claude Code 🤖
