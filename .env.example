# Production Environment Variables Template
# Copy to .env.slon.prvms.ru (or your domain) and fill with your values
# ⚠️ NEVER commit this file with real credentials to git!

# ==============================================================================
# NGINX and SSL Settings (REQUIRED for production!)
# ==============================================================================
DOMAIN=your-domain.com
SSL_EMAIL=admin@your-domain.com

# ==============================================================================
# Django Core Settings
# ==============================================================================
# Generate SECRET_KEY with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# or: openssl rand -base64 60
SECRET_KEY=your-secret-key-minimum-50-characters-long-CHANGE-THIS

DEBUG=False

# Domain Settings
ALLOWED_HOSTS=your-domain.com
CSRF_TRUSTED_ORIGINS=https://your-domain.com

# ==============================================================================
# Database Settings
# ==============================================================================
DB_NAME=elephant_shop
DB_USER=postgres
# Generate strong password with: openssl rand -base64 32
DB_PASSWORD=your-strong-db-password-CHANGE-THIS
DB_HOST=db
DB_PORT=5432

# ==============================================================================
# Redis Settings (NEW - REQUIRED for production!)
# ==============================================================================
# Generate with: openssl rand -base64 24
REDIS_PASSWORD=your-redis-password-CHANGE-THIS
REDIS_HOST=redis
REDIS_PORT=6379

# Redis URLs will be auto-generated from REDIS_PASSWORD
# Don't set these manually unless you have custom Redis setup
# CELERY_BROKER_URL=redis://:${REDIS_PASSWORD}@redis:6379/0
# CELERY_RESULT_BACKEND=redis://:${REDIS_PASSWORD}@redis:6379/0

# ==============================================================================
# OAuth Settings (Get from provider dashboards)
# ==============================================================================

# Google OAuth (https://console.cloud.google.com/apis/credentials)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret-CHANGE-THIS

# Apple Sign In (https://developer.apple.com/account/resources/identifiers)
APPLE_CLIENT_ID=com.yourcompany.kupi-slona
APPLE_TEAM_ID=your-apple-team-id
APPLE_KEY_ID=your-apple-key-id
# Note: Use actual newlines in APPLE_PRIVATE_KEY or escape them as \n
APPLE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----

# ==============================================================================
# Optional: Monitoring & Observability
# ==============================================================================
# FLOWER_PASSWORD=your-flower-password-for-celery-monitoring

# ==============================================================================
# Optional: Backup Configuration
# ==============================================================================
# BACKUP_DIR=/backups/postgres
# RETENTION_DAYS=7
