# Известные проблемы

На текущий момент (v0.1.0) критических известных проблем нет.

## Отслеживаемое

- Некоторые пункты из README TODO остаются нереализованными (см. `docs/TASK_STATE.md` → Бэклог).
- Apple Sign In не активирован в `INSTALLED_APPS` / `SOCIALACCOUNT_PROVIDERS` (только Google OAuth работает), несмотря на наличие переменных в `.env.example`.
- `README.md` содержит legacy-упоминание `elephant_shop/` вместо актуального `kupi_slona/` в некоторых местах структуры проекта.

При обнаружении новых багов — добавлять сюда с датой, severity и ссылкой на задачу.
